[phases.setup]
nixPkgs = ["jdk17"]

[phases.install]
cmds = ["mvn clean package -DskipTests"]

[phases.build]
cmds = ["echo 'Build completed'"]

[start]
cmd = "java -jar target/edu-0.0.1-SNAPSHOT.jar"
